Cделано с помощью AngularJs, логика выборки написана отдельной функцией фильтром(откомментировал как мог=)). Сделал в три колонки так как текста очень мало было на всю ширину страницы. Проверял на OpenServer всё работало. По поиску при всех ваших тестовых примерах всё отображаеться как нужно, единственное в примере Central Midfield "England" для корректной работы кавычки нужно убрать(фильтрацию по неккоректным символам не делал), сортируеться по релевантности по принципу количества совпадающих символов.
